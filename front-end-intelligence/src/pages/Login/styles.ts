import { styled } from '../../styles/theme';

export const Container = styled.div`
	display: flex;
	flex-direction: column;
	align-items: center;

	form {
		width: 90%;
		max-width: 900px;
		margin-top: 50px;
	}
`;
